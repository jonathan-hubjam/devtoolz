import PrivacyPage from '@/pages/PrivacyPage';

export const metadata = {
  title: 'Privacy Policy | DevToolz',
  description: 'How DevToolz handles your data — spoiler: almost nothing is collected and everything runs in your browser.',
};

export default function Privacy() {
  return <PrivacyPage />;
}
