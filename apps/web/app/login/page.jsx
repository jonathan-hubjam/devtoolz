import LoginPage from '@/pages/LoginPage';

export const metadata = {
  title: 'Admin Login – DevToolz',
  description: 'Login to access protected settings and admin features.',
};

export default function Login() {
  return <LoginPage />;
}
